// Adicionando elementos (.push)

let scores = [10, 6.5, 8, 7.5];

scores.push(9);

console.log(scores);

let average =
  (scores[0] + scores[1] + scores[2] + scores[3] + scores[4]) / scores.length;

console.log(average);
